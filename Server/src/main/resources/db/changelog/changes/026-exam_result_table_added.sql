-- liquibase formatted sql
-- changeset PK:026-exam_result_table_added.sql

CREATE TABLE exam_attempt (
    id INT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    started_at TIMESTAMP NOT NULL,
    finished_at TIMESTAMP,
    finished BOOLEAN NULL DEFAULT FALSE,
    correct_answers INT DEFAULT 0,
    total_questions INT DEFAULT 0,
    points INT DEFAULT 0,
    total_points INT DEFAULT 0,
    percentage NUMERIC DEFAULT 0,
    grade NUMERIC DEFAULT 0,
    bonus_minutes_used BOOLEAN DEFAULT FALSE,
    bonus_points_used BOOLEAN DEFAULT FALSE,
    remove_option_used BOOLEAN DEFAULT FALSE,
    exam_id INT NOT NULL REFERENCES exams(id) ON DELETE CASCADE,
    user_id INT NOT NULL REFERENCES users(id) ON DELETE CASCADE
);